[package]
name = "shuttle-rs"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust port of Shuttle macOS menu-bar app"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
dirs = "5"

[dev-dependencies]
tempfile = "3"
